## Work Log Entry: 2026-04-17

Clock in: 9:00 a.m.
Clock out: 2:28 p.m.

### Summary

Shipped desktop sidebar collapse with localStorage preference persistence — 
hide/show from the sidebar header chevron or the hamburger that appears top-left of the main area when closed. 
Added the "Deep Research" hero-box toggle. When active, the agent's first round gets tool_choice: { function: { name: "web_research" } }, forcing the model into Research instead of falling back to multi-query web_search
recovery. However the call still does not really work. we will have to fix it next week. 
Investigated a data-integrity bug where switching conversations or navigating away mid-stream could corrupt the loaded view (the in-flight reader was appending tokens into whatever messages array was now on screen). Attempted to detach the reader cleanly on navigation and implement server-side capture, but the fix is not yet working and will require further investigation.
Known issue documented and parked: scroll-up-while-streaming still snaps the
viewport back to bottom after two implementation attempts (state-tracked isAtBottom, then fresh read inside the scroll effect). Likely browser
scroll-anchoring interaction. Revisiting next week alongside the inline agent-activity indicators ("searching…", "accessed 4 sources") since that work
involves refactoring the streaming layer anyway.