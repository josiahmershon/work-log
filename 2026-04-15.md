## Work Log Entry: 2026-04-15

Clock in: 9:43 a.m.
Clock out: 6:34 p.m.

### Summary

Caught two production bugs in the new Research integration via the analytics
dashboard. First: the submit call was failing instantly with "Unauthorized:  
missing API key in Authorization header" this was Tavily's /research endpoint
requires Authorization: Bearer <key>, unlike /search and /extract which accept
api_key in the request body. Patched both the submit and the poll to use
Bearer. Second: the 150s poll ceiling wasn't enough Tavily's auto model tier
picks the pro model for substantive queries, which can exceed 180s. Bumped
the timeout to 300s and added periodic progress logging so it's clear whether
Tavily itself is slow vs. a real error.
Scoop perf pass: wrapped ChatMessage in React.memo, so ReactMarkdown stops
re-parsing every stable message on every streamed token of the current turn.  
Visible snappiness improvement during long streams and long conversations.